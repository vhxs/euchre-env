[project]
name = "euchre"
version = "0.1.0"
description = "Euchre game environment."
authors = [{ name = "anon", email = "vikram.saraph.22@gmail.com" }]
license = { text = "MIT" }
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
dev = []
test = []

[tool.uv]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
